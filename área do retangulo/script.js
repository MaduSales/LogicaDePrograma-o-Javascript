let base;
let altura;
let area;

base = parseInt(prompt("Digite o valor da base"));
altura = parseInt(prompt("Digite o valor da altura"));

area = base * altura;

alert(`Com a base ${base}cm e a altura ${altura}cm, a área será ${area}`)